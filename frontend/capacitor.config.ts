import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.premierFantasySports.fantasyFootballApp',
  appName: 'fantasyFootballApp',
  webDir: 'dist'
};

export default config;
